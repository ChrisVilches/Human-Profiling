<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="MainWindow" Height="600" Width="1000">
    <Grid>



        <Button Content="Refresh"
                HorizontalAlignment="Left"
                Margin="10,10,0,0"
                VerticalAlignment="Top"
                Width="75"
                Click="Refresh_Click"/>

        <local:PieChart x:Name="SwitchPieChart" HorizontalAlignment="Left" VerticalAlignment="Top" Height="350" Width="350" Margin="150,50,0,0"/>

        <local:PieChart x:Name="MostUsedChart" HorizontalAlignment="Left" VerticalAlignment="Top" Height="350" Width="350" Margin="500,50,0,0"/>

        <Grid Margin="10,50,0,0" Width="350" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="Total"/>
            <Label Grid.Row="1" Grid.Column="0" Content="Today"/>
            <Label Grid.Row="2" Grid.Column="0" Content="Hour"/>
            <Label Grid.Row="0" Grid.Column="1" Content="{Binding TotalCount}"/>
            <Label Grid.Row="1" Grid.Column="1" Content="{Binding TodayCount}"/>
            <Label Grid.Row="2" Grid.Column="1" Content="{Binding HourCount}"/>

        </Grid>

    </Grid>

</Window>
